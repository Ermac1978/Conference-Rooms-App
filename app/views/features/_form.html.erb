<%= form_for(@feature) do |f| %>
  <% if @feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feature.errors.count, "error") %> prohibited this feature from being saved:</h2>

      <ul>
      <% @feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :feature %><br>
    <%= f.text_field :feat %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<center><%= link_to 'Go back to your rooms', conf_rooms_path, class: 'btn btn-success' %></center>