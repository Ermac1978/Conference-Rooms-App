    <ul class="nav nav-tabs" role="tablist">
      <% if signed_in? %>
        <li role="presentation" ><%= link_to 'Home', root_path %></li>
        <li role="presentation" class="active"><%= link_to 'Conference Rooms', conf_rooms_path %></li>
          <% if signed_in? && current_user.staff? %>
            <li role="presentation" ><%= link_to 'Users', users_path %></li>
          <% end %>
      <% end %>
    </ul>

